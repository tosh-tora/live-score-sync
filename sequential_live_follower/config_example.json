{
  "settings": {
    "cooldown_seconds": 3.0,
    "confidence_threshold": 0.4,
    "silence_threshold_db": -55.0,
    "inertia_timeout_seconds": 5.0,
    "mic_device": "pulse",
    "_comment_matcher_kwargs": "Optional. Overrides for pymatchmaker's OLTW Arzt internals. Defaults (window_size=30, step_size=1, start_window_size=8) are tuned for scores with repeating motifs (e.g. Beethoven 5). Larger values allow faster tempo deviations but risk runaway alignment. Omit this field to use the defaults.",
    "matcher_kwargs": {
      "window_size": 30,
      "step_size": 1,
      "start_window_size": 8
    }
  },
  "movements": [
    {
      "id": 1,
      "xml_file": "guide_mv1.xml",
      "triggers": [
        {
          "measure": 1,
          "action": "right",
          "note": "Movement 1 - Start"
        },
        {
          "measure": 45,
          "action": "right",
          "note": "Theme A"
        },
        {
          "measure": 89,
          "action": "right",
          "note": "Coda"
        }
      ]
    },
    {
      "id": 2,
      "xml_file": "guide_mv2.xml",
      "triggers": [
        {
          "measure": 1,
          "action": "right",
          "note": "Movement 2 - Start"
        },
        {
          "measure": 67,
          "action": "left",
          "note": "Return to theme"
        }
      ]
    }
  ]
}
