# Core dependencies (cross-platform)
music21>=8.0
partitura>=0.3.10
librosa>=0.9.0
numpy>=1.20
pandas>=1.0
scikit-learn>=0.20

# Task 2 (Sequential Live Follower) runtime - requires WSL2 / Linux
# pymatchmaker does not publish Windows wheels; install inside WSL2.
pymatchmaker>=0.2.1
playwright>=1.40
sounddevice>=0.4.6

# Note: the 'N' key for movement advance is bound on the Tkinter operator
# window (focused-window key binding); no system-level hotkey library is
# required. The previous `keyboard` and `pyautogui` dependencies are no
# longer used.
